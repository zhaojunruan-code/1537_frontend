### 2024-12-9
1. 更新 `env`
2. 新增 `usePolling` 通用轮询钩子函数
3. 优化 GET 请求
4. 优化接口代理

### 2024-10-29
更新 `objectToQueryParams` 函数，现在只有在 `H5` 端才会对参数进行编码，其他段保持原文

### 2024-10-23
1. 优化登陆包装的样式
2. 引入字典工具，使用例子请看 readme.md
3. 修复接口生成后 get方法的函数参数会被意外添加引号的bug
4. 修复pinia在nvue下报错白屏的bug
5. 更新接口生成脚本，现在生成的接口会以 模块 + 接口功能 的命名方式生成
6. 优化网络请求代码

### 2024-10-10
更新接口生成脚本，现在生成的接口会以 模块 + 接口功能 的命名方式生成。比如下面这样
```js
// 旧版本
// /api/index/correctDelete
function correctDelete() {
  // 略
}

// 新版本
// /api/index/correctDelete
function indexCorrectDelete() {
  // 略
}

// /mk/floors/create
function floorsCreate() {
  // 略
}
```
